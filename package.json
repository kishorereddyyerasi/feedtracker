{
  "name": "feedtracker",
  "version": "1.0.0",
  "description": "Newborn FeedTracker App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step - pure HTML/JS'",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
